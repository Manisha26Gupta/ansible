<!doctype html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <title>{{ app_name }} ({{ env }})</title>
  </head>
  <body>
    <h1>It works! 🎉</h1>
    <p>Welcome to <strong>{{ app_name }}</strong> (environment: <em>{{ env }}</em>).</p>
    <p>Served from: <code>{{ doc_root }}</code></p>
  </body>
</html>

